/**
 * 8.	O programa “ao quadrado” imprime os quadrados dos números de um a nove, 
 * separando cada expressão em uma linha.
 */

  for(let i = 1; i < 10 ; i++){
      let quadrado = i * i
      console.log(i + " ^ 2 = " + quadrado);
  }